<div class="container">
  <h1>Movies Lists</h1>
  <% @lists.each do |list| %>
    <%= link_to list.name, list_path(list) %>
    <%= link_to "Delete List",
      list_path(list),
      data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
    %>
  <% end %>
</div>
